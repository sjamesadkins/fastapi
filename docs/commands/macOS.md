# **Apple Command Reference**

This reference guide provides information about the `windows` command and its usage.


- **Clear Terminal:**
    ```bash
    command+k
    ```

cd scripts

chmod u+x db-setup.sh

ls -l db-setup.sh